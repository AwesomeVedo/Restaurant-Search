import React from "react";
import { View, Text, Image, StyleSheet } from 'react-native';

const SingleResult = () => {
    return (
        <View><Text>Single Result</Text></View>
    );
};

const styles = StyleSheet.create({

});

export default SingleResult;